{
  "name": "sftp-relay-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "up": "docker compose up -d --build",
    "down": "docker compose down -v",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
